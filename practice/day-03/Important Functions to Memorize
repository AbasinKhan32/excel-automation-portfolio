# Missing values
df.isna()
df.isna().sum()
df.dropna()
df.fillna()

# Duplicates
df.duplicated()
df.drop_duplicates()

# Text cleaning
.str.strip()
.str.lower()
.str.upper()
.str.title()

# Column names
df.columns
df.rename()

# Data types
df.dtypes
.astype()
pd.to_numeric()
pd.to_datetime()

# Data inspection
df.head()
df.info()
df.describe()
df.unique()
df.nunique()
df.value_counts()

# Removing data
df.drop()

# Filtering
df.loc[]
df[condition]

# Sorting
df.sort_values()

# Index
df.reset_index()

# Saving
df.to_excel()
